import React from 'react';

import './spinner.scss';

export default () => {
    return (
        <div className="spinner">
            <div className="spinner__animation" />
        </div>
    );
};
